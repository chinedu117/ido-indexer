# RPC (use WebSocket endpoint for realtime subscriptions)
RPC_WS=wss://eth-sepolia.g.alchemy.com/v2/YOUR_KEY
# Or use ws://localhost:8546 for a local node

# MongoDB connection string
MONGO_URI=mongodb://mongo:27017/ido

# Redis connection for BullMQ (optional but recommended)
REDIS_HOST=redis
REDIS_PORT=6379

# Contract & indexing
CONTRACT_ADDRESS=0xYourIDOContractAddress
START_BLOCK=0
CONFIRMATIONS=6
BATCH_SIZE=1000

# Misc
LOG_LEVEL=info